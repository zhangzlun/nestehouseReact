export enum FormType {
    button = 'button',
    checkbox = 'checkbox',
    date = 'date',
    email = 'email',
    file = 'file',
    hidden = 'hidden',
    image = 'image',
    password = 'password',
    radio = 'radio',
    range = 'range',
    submit = 'submit',
    text = 'text',
    url = 'url',
}

export enum FormElem {
    select = 'select',
    input = 'input',
}
